[project]
name = "agent-workspace-backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
# Note: MCP_server/requirements.txt packages (fastmcp, edge-tts, etc.) have been installed into this main project's virtual environment.
dependencies = [
    "agent-evaluator",
    "alembic>=1.18.5",
    "fastapi>=0.141.1",
    "google-genai>=2.18.1",
    "jinja2>=3.1.6",
    "jsonpatch>=1.33",
    "langchain-community>=0.4.2",
    "langchain-litellm>=0.7.0",
    "langchain-mcp-adapters>=0.3.2",
    "langgraph>=1.2.11",
    "mcp>=1.29.0",
    "psycopg2-binary>=2.9.12",
    "pydantic>=2.13.4",
    "sqlalchemy>=2.0.51",
    "uvicorn[standard]>=0.52.0",
    "langgraph>=0.1.0",
    "langchain-mcp-adapters>=0.3.0",
    "mcp>=1.0.0"
]

[tool.uv.sources]
agent-evaluator = { path = "../Agent_evaluator", editable = true }
